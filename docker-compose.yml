version: '3.8'

services:
  home-device-bridge:
    build: .
    container_name: home-device-bridge
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - bridge-network

networks:
  bridge-network:
    driver: bridge
